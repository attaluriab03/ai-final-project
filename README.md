# ai-final-project

## 4 Models In Use
- Regularized Logistic Regression Model (traditional model for tabular data)
- XGBoost Model (tree-based ensemble method)
- FT-Transformer (DL-based transformer model - fully trained)
- TabPFN (Modern transformer model most similar to LLMs for Tabular Data)